#!/bin/sh
set -e
	update-alternatives --install /usr/bin/python3.7m-config python-config /usr/bin/python3.7m-config-lib 10
if [ x"$D" = "x" ]; then
	if [ -x /sbin/ldconfig ]; then /sbin/ldconfig ; fi
fi
